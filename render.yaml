services:
  - type: web
    name: pakistan-jobs-session-generator
    env: node
    plan: free
    buildCommand: npm install
    startCommand: node index.js
    envVars:
      - key: NODE_ENV
        value: production
